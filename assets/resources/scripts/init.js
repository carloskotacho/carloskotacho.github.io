$(document).ready(function(){
    // Init Mobile Collapse Button
    $(".button-collapse").sideNav();
    // Init of Plugin jQuery
    $('.scrollspy').scrollSpy();
    // Init of Chips
    $('.chips').material_chip();
});